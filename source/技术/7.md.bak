---
title: "Rust 7机制"
createTime: 2024-01-07 10:00:00
tags: ["Rust,编程"]
---

# Rust 迭代器

## 惰性求值特性
```rust
let nums = vec![1, 2, 3];
let squares: Vec<_> = nums.iter().map(|x| x * x).collect();
```

## 常见适配器
- filter 过滤元素
- take 限制数量
- zip 合并迭代器
- chain 连接迭代器

| 方法        | 时间复杂度 |
|------------|------------|
| map        | O(1)       |
| filter     | O(1)       |
| collect    | O(n)       |