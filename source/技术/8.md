# Rust 智能指针

## Box 堆分配
```rust
fn main() {
    let b = Box::new(5);
    println!("b = {}", b);
}
```

## Rc 引用计数
- 多所有权场景
- 不可变引用共享
- 线程不安全

| 指针类型 | 线程安全 | 可变性 |
|----------|----------|--------|
| Box<T>   | ✅       | 独占   |
| Rc<T>    | ❌       | 共享   |
| Arc<T>   | ✅       | 共享   |