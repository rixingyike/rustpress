---
title: "Web开发实战"
createTime: 2024-01-21 10:00:00
tags: ["后端开发", "Rust", "Actix-web"]
---

# Web开发实战

## Actix-web 示例
```rust
use actix_web::{get, web, App, HttpResponse, HttpServer, Responder};

#[get("/{id}/{name}")]
async fn index(info: web::Path<(u32, String)>) -> impl Responder {
    HttpResponse::Ok().body(format!("Hello {}! id:{}", info.1, info.0))
}
```

## 路由配置
| 方法    | 路径           | 处理器     |
|---------|----------------|------------|
| GET     | /api/data      | get_data   |
| POST    | /api/upload    | post_file  |
| PUT     | /api/update    | update_item|