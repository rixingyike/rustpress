# 1. 2026-06-23 用户需求记录

用户提出了以下需求：
1. **支持短tweet**：像X那样，可以发没有标题的一段话，可以有图片；
2. **支持长文与短文**：RustPress要能写长，也能写短；
3. **支持打造专栏**：专栏是一个文章系列，类似极客时间，具有list列表和文件树导航；
4. **缩短blog文章链接**：支持将文章在线链接缩短，例如从 `https://yishulun.com/blog/2026/40.html` 缩短为 `https://yishulun.com/2026/40.html`；
5. **支持多Themes**：保留原 `default` 主题，新功能与新特征放在 `light` 样式主题中。

# 2. 2026-06-23 用户新需求记录

用户提出了以下新需求：
- 不喜欢文字文件名，以后这个文件名可能是软件生成的。需要将 `tweet-test.md` 改为数字或时间戳作为文件名（如 `202606230940.md` 或数字）。

# 3. 2026-06-23 首页图片路径转义问题

用户提出：在首页上，多出了一个 img，src 是 `&#x2F;static&#x2F;images&#x2F;avatar.png`，导致图片不显示，为什么？

# 4. 2026-06-23 短 tweet 社交按钮交互实现（点赞、评论等）

用户提出：每个 tweet 下面有四个按钮（评论、分享/转发、点赞、浏览量），在没有数据库的静态网页中，该如何实现？特别是评论部分目前是保存在 GitHub Issues 上的。

# 5. 2026-06-23 调整 light 主题配色与 UI 风格

用户提出：使用 `docs/light 配色方案.md` 中的配色方案，调整 light theme 的配色和 UI 风格。

# 6. 2026-06-23 修改 archives.html 页面大蓝按钮配色

用户提出：/archives.html 这个页面中，查看“2026年文章”等的大蓝按钮配色需要修改，认为像工作服一样，要求与全站极光薄荷 3.0 的整体配色保持一致。

# 7. 2026-06-23 首页 Tweet 状态微调（去除底部交互与昵称，日期带时间并链接至独立地址）

用户提出：
1. 首页上的 Tweet 状态，由于是静态页面，不需要底部的评论、收藏、点赞、阅读量等死按钮，予以去掉。
2. 内容上方保留头像和昵称，但去掉 `@LIYI` (即 @ 账户名)，因为整个博客都是自己的。
3. 后面的日期需要加上时间，并且点击日期时间可以跳转到该 Tweet 自身的独立页面地址。

